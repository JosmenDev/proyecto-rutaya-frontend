import 'package:flutter/material.dart';

class MapTripContent extends StatelessWidget {
  const MapTripContent({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text('MapContent'),
    );
  }
}
